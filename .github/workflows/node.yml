name: MESSAGING_APP CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build:
    env:
      CI: true
    strategy:
      node-version: [16.x, 18.x, 20.x]
    runs-on: ubuntu-latest
    steps:
